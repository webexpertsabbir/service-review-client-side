import React from 'react';
import useTitle from '../../hooks/useTitle';

const MyReviews = () => {
    useTitle('My Reviews')
    return (
        <div>
            <h2>My Reviews</h2>
        </div>
    );
};

export default MyReviews;